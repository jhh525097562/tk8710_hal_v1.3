# TK8710 当前工程编译与结构说明

## 1. 当前结构

当前工程已经从旧的聚合头/兼容接口，收敛到以下主结构：

1. 对外 HAL 接口：`inc/hal_api.h`
2. 对外 PHY 接口：`inc/phy/phy_api.h`
3. TRM 公共接口：`inc/trm/trm_api.h`
4. 内部底层仍保留 `src/driver/*` 作为 PHY 封装承载

当前主要目录：

- `inc/hal_api.h`
- `inc/phy/*`
- `inc/trm/*`
- `src/hal/*`
- `src/phy/*`
- `src/trm/*`
- `src/driver/*`

已删除的旧兼容壳头：

- `inc/trm/trm_config.h`
- `inc/trm/trm.h`
- `inc/driver/tk8710.h`

## 2. 推荐编译方式

### 2.1 RK3506（推荐）

在 WSL Ubuntu 中编译。

Windows 工程路径：

`F:\opencode\02-new-projects\02-embedded-software\tk8710`

WSL 对应路径：

`/mnt/f/opencode/02-new-projects/02-embedded-software/tk8710`

### 2.2 环境初始化

交叉编译 SDK 已安装在：

`/home/huhengzhuo/arm-buildroot-linux-gnueabihf_sdk-buildroot`

每次编译前先执行：

```bash
source /home/huhengzhuo/arm-buildroot-linux-gnueabihf_sdk-buildroot/environment-setup
```

### 2.3 RK3506 全量编译命令

```bash
cd /mnt/f/opencode/02-new-projects/02-embedded-software/tk8710
source /home/huhengzhuo/arm-buildroot-linux-gnueabihf_sdk-buildroot/environment-setup
./cmake/build_rk3506.sh
```

编译产物输出到：

`build_rk3506/`

主要产物包括：

- `libtk8710_hal_complete.a`
- `test8710main_3506`
- `TestTRMmain`
- `TestTRMmain_IPC`
- `Test8710Loopback`
- `Test8710Master`
- `Test8710Master_Calibrate`
- `Test8710RFTxTone`
- `Test8710RxSensitivity`
- `Test8710Slave`
- `Test8710W_R_Reg`

## 3. Windows/JTOOL 编译方式

在 Windows PowerShell / CMD 的工程根目录执行：

```bash
mingw32-make -f cmake/Makefile.jtool clean
mingw32-make -f cmake/Makefile.jtool all
```

产物输出到：

`build_jtool/`

## 4. 当前验证状态

当前代码已经验证通过：

1. RK3506 全量编译通过
2. JTOOL 主构建链通过
3. 删除兼容壳头后，RK3506 全量编译仍通过

## 5. 当前主入口头文件

### HAL 层

- `inc/hal_api.h`

### PHY 层

- `inc/phy/phy_api.h`
- `inc/phy/phy_irq.h`
- `inc/phy/phy_regs.h`
- `inc/phy/phy_log.h`

### TRM 层

- `inc/trm/trm_api.h`
- `inc/trm/trm_internal.h`
- `inc/trm/trm_beam.h`
- `inc/trm/trm_data.h`
- `inc/trm/trm_queue.h`
- `inc/trm/phy_cfg.h`
- `inc/trm/phy_data.h`
- `inc/trm/phy_stat.h`
